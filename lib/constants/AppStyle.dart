import 'package:flutter/material.dart';

import 'AppColors.dart';

class AppStyle {
  // static var textFormPadding = EdgeInsets.symmetric(
  //     horizontal: 20 * SizeConfig.horizontalBlock,
  //     vertical: 16 * SizeConfig.verticalBlock);
  // static const authButtonPadding = EdgeInsets.all(15);
  // static final containerBorderRadius =
  //     BorderRadius.circular(10 * SizeConfig.textRatio);
  // static final pagePadding = EdgeInsets.symmetric(horizontal: 2.w);
  // static final productsListAndCategory = EdgeInsets.symmetric(horizontal: 2.w);
  // static final bottomPadding =
  //     EdgeInsets.symmetric(horizontal: 4.w, vertical: 1.h);
  // static final bottomItemsPadding =
  //     EdgeInsets.symmetric(horizontal: 5.w, vertical: 1.h);
  // static final buttonBlackBorder =
  //     Border.all(width: SizeConfig.textRatio, color: AppColors.firstBlackColor);
}
